# Portfolio Tracker Boilerplate

Python desktop app starter (Qt + SQLite) for a portfolio manager home page inspired by your reference design.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

SQLite DB file is created at `data/portfolio.db`.
# PortfolioTracker
